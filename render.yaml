services:
  - type: web
    name: comicbook-api
    runtime: node
    branch: main
    plan: starter
    buildCommand: |
      npm install
      npx prisma generate
      npx prisma migrate deploy || npx prisma migrate dev --name init
      npm run seed
    startCommand: npm start
    
